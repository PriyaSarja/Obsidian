# ============================================================
# Model Server — dependencies only (not the full repo stack)
# ============================================================

# Web framework & server
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0,<1.0
python-multipart>=0.0.12,<1.0

# HTTP client (health endpoint, model download)
httpx>=0.27.0,<1.0

# ---------------------------------------------------------------
# ML / Embeddings
# torch is pinned to 2.5.x — later versions (2.12+) pull >1 GB
# of CUDA extras (cuDNN, NCCL, NVSHMEM, Triton) that bloat the
# image and cause pip timeouts.  Change to torch==2.5.1+cu121
# (or similar) if you need GPU acceleration.
# ---------------------------------------------------------------
torch==2.5.1
torchvision==0.20.1
sentence-transformers>=3.3.0,<3.5
transformers>=4.40.0,<4.50
timm>=1.0.0,<2.0
Pillow>=11.0.0,<12.0
einops>=0.7.0,<1.0

# Config / env
python-dotenv>=1.0.1,<2.0
pydantic-settings>=2.6.0,<3.0

# Structured logging
structlog>=24.4.0,<25.0
